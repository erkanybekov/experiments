Вот короткая формула для `Task` в Swift:

1. **Не писать `<Void, Never>` явно**, если:

   * задача **ничего не возвращает** (`Void`)
   * **ошибок не ожидается** (`Never`)
   * просто хотите запустить асинхронный код

```swift
Task {
    await doSomething()
}
```

2. **Писать `<ReturnType, ErrorType>` явно**, если:

   * нужно вернуть конкретный тип
   * нужно явно указать возможные ошибки
   * хотите передать `Task` куда-то с конкретным типом

```swift
let task: Task<String, Error> = Task {
    try await fetchData()
}
```

💡 **Правило на пальцах:** если результат и ошибки не важны — пишите просто `Task { }`.
